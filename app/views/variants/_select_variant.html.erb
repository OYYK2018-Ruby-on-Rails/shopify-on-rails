<%= form_tag(gift_product_path(product), method: :post, remote: true) do %>
  <%= select_tag :variant_id, options_for_select(product.variants.in_stock.collect { |variant| [variant.name, variant.id] }) %>
  <%= submit_tag('Send Gift') %>
<% end %>